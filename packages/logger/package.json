{
  "name": "logger",
  "version": "0.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "tsup",
    "watch": "tsup --watch",
    "lint": "eslint src/",
    "lint-pre-commit": "eslint src/ --max-warnings=0"
  },
  "devDependencies": {
    "@types/node": "^18.11.9",
    "eslint-config-custom": "workspace:*",
    "tsconfig": "workspace:*",
    "tsup": "^7.2.0",
    "typescript": "^5.2.2"
  }
}
