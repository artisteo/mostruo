// endpoint will be here
const LOGIN_POST_PATH = "/api/auth/login";

// its weird we have to do this because the real endpoint is determined in app folder
// how can we use this const instead of app folder structures creating api/auth/login ?

export default LOGIN_POST_PATH;
