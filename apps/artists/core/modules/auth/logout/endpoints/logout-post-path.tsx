// endpoint will be here
const LOGOUT_POST_PATH = "/api/auth/logout";

// its weird we have to do this because the real endpoint is determined in app folder
// how can we use this const instead of app folder structures creating api/auth/logout ?

export default LOGOUT_POST_PATH;
